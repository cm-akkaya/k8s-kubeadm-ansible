---
- name: Create apt keyring directory
  ansible.builtin.file:
    path: /etc/apt/keyrings
    state: directory
    mode: "0755"

- name: Remove legacy Docker repository definition
  ansible.builtin.file:
    path: /etc/apt/sources.list.d/docker.list
    state: absent

- name: Configure Docker repository
  ansible.builtin.deb822_repository:
    name: docker
    types:
      - deb
    uris:
      - https://download.docker.com/linux/ubuntu
    suites:
      - "{{ ansible_facts['distribution_release'] }}"
    components:
      - stable
    architectures:
      - amd64
    signed_by: https://download.docker.com/linux/ubuntu/gpg
    state: present

- name: Update apt cache after Docker repository configuration
  ansible.builtin.apt:
    update_cache: true

- name: Install containerd
  ansible.builtin.apt:
    name: "containerd.io={{ containerd_package_version }}"
    state: present
    allow_downgrade: true
    allow_change_held_packages: true
    update_cache: true

- name: Hold containerd
  ansible.builtin.dpkg_selections:
    name: containerd.io
    selection: hold      

- name: Create containerd directory
  ansible.builtin.file:
    path: /etc/containerd
    state: directory
    mode: "0755"

- name: Configure containerd
  ansible.builtin.template:
    src: config.toml.j2
    dest: /etc/containerd/config.toml
    owner: root
    group: root
    mode: "0644"
  notify: Restart containerd

- name: Enable containerd
  ansible.builtin.service:
    name: containerd
    enabled: true
    state: started

- name: Apply containerd configuration
  ansible.builtin.meta: flush_handlers

- name: Hold containerd
  ansible.builtin.dpkg_selections:
    name: containerd.io
    selection: hold

- name: Verify containerd configuration
  ansible.builtin.command:
    cmd: containerd config dump
  register: containerd_config
  changed_when: false

- name: Verify SystemdCgroup
  ansible.builtin.assert:
    that:
      - "'SystemdCgroup = true' in containerd_config.stdout"
    fail_msg: "containerd SystemdCgroup=true değil."
